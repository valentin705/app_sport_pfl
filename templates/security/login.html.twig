{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}
{% block stylesheets %}
{{ parent() }}
<link rel="stylesheet" href="{{ asset('css/login.css') }}">
{% endblock %}
{% block body %}
<div class="container min-vh-100 d-flex align-items-center">
    <div class="card">
        <div class="row g-0 h-100">
            <div id="login-register-div" class="col-md-8 d-flex position-relative">
                <img id="login-register-img" src="{{ asset('images/login-register-img.jpg')}}"
                    class="img-fluid rounded-start" alt="...">
                <div id="absolute-text" class="position-absolute px-3 d-flex flex-column-reverse">
                    <p>SportNet est une application de sport novatrice qui vise à créer un réseau social dédié aux
                        amateurs de sport. Notre objectif est de fournir une plateforme conviviale où les utilisateurs
                        peuvent partager leurs séances de sport, interagir avec d'autres passionnés et construire des
                        liens forts au sein de la communauté sportive.</p>
                </div>
            </div>
            <div class="col-md-4">
                <div class="card-body" style="height: 100%;">
                    <form method="post" class="d-flex flex-column" style="height: 100%;">
                        {% if error %}
                        <div class="alert alert-danger">
                            Vos informations sont incorrectes
                        </div>
                        {% endif %}

                        {% if app.user %}
                        <div class="mb-3">
                            You are logged in as {{ app.user.userIdentifier }}, <a
                                href="{{ path('app_logout') }}">Logout</a>
                        </div>
                        {% endif %}
                        {% for message in app.flashes('successAccount') %}
                        <div class="alert alert-success">
                            {{ message }}
                        </div>
                        {% endfor %}
                        <h1 class="h3 mb-3 mt-5 font-weight-normal" style="text-align: center;">Connexion</h1>
                        <div class="mb-5">
                            <label for="inputEmail">Email</label>
                            <input type="email" value="{{ last_username }}" name="email" id="inputEmail"
                                class="form-control" autocomplete="email" placeholder="Entrez votre email" required
                                autofocus>
                        </div>
                        <div class="mb-5">
                            <label for="inputPassword">Mot de passe</label>
                            <input type="password" name="password" id="inputPassword" class="form-control"
                                autocomplete="current-password" placeholder="Entrez votre mot de passe" required>
                            <p class="mt-1" style="font-size:0.8em;">Pas encore de compte ? <a
                                    class="login-register-link" href="{{ path('app_register') }}">Inscris
                                    toi !</a></p>
                        </div>

                        <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

                        {#
                        Uncomment this section and add a remember_me option below your firewall to activate remember me
                        functionality.
                        See https://symfony.com/doc/current/security/remember_me.html

                        <div class="checkbox mb-3">
                            <label>
                                <input type="checkbox" name="_remember_me"> Remember me
                            </label>
                        </div>
                        #}


                        <button class=" mb-5 btn btn-secondary" type="submit">
                            Connexion
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
{% block javascripts %}
{{ parent() }}
<script src="{{ asset('js/login.js') }}"></script>
{% endblock %}