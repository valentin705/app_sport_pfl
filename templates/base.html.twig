<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="icon"
        href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
    {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
    {% block stylesheets %}
    {{ encore_entry_link_tags('app') }}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">
    <link rel="stylesheet" href="{{ asset('css/style.css') }}">
    {% endblock %}
</head>

<body>
    <!-- Main Navigation -->
    {# <nav>
        <div class="nav-left">
            <div class="img-crop">
                <img src="{{ asset('images/SportNetLogo.png')}}" alt="" id="logo">
            </div>
            {% if app.user is not null %}
            <a href="{{ path('home_list') }}" class="nav-item">Acceuil</a>
        </div>
        <div>
            <a href="{{ path('home_list') }}" class="nav-item">Mon profil</a>
            <a href="{{ path('home_list') }}" class="nav-item">Recherche</a>
            <a href="{{ path('app_logout') }}" class="nav-item">Déconnection</a>
        </div>
        {% else %}
        <a href="{{ path('app_login') }}" class="nav-item">Acceuil</a>
        </div>
        <div>
            <a href="{{ path('app_register') }}" class="nav-item">Inscription</a>
            <a href="{{ path('app_login') }}" class="nav-item">Connexion</a>
        </div>
        {% endif %}
    </nav> #}



    <nav class="navbar navbar-expand-lg fixed-top">
        <div class="container-lg">
            <div class="collapse navbar-collapse" id="navbarText">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="img-crop nav-item">
                        <a class="navbar-brand"
                            href="{% if app.user is not null %} {{ path('home_list') }} {% else %} {{ path('app_login') }} {% endif %}">
                            <img id="logo" src="{{ asset('images/logoV1.png')}}" alt="">
                        </a>
                    </li>
                    {% if app.user is not null %}
                    <li class="nav-item">
                        <a href="{{ path('home_list') }}" class="nav-link">Acceuil</a>
                    </li>
                </ul>
                <span class="navbar-text">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <li class="nav-item">
                            <a href="{{ path('home_list') }}" class="nav-link">Mon profil</a>
                        </li>
                        <li class="nav-item">
                            <a href="{{ path('home_list') }}" class="nav-link">Recherche</a>
                        </li>
                        <li class="nav-item">
                            <a href="{{ path('app_logout') }}" class="nav-link">Déconnection</a>
                        </li>
                    </ul>
                </span>
                {% else %}
                </ul>
                <span class="navbar-text">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <li class="nav-item">
                            <a href="{{ path('app_register') }}" class="nav-link">Inscription</a>
                        </li>
                        <li class="nav-item">
                            <a href="{{ path('app_login') }}" class="nav-link">Connexion</a>
                        </li>
                    </ul>
                </span>
                {% endif %}
            </div>
        </div>
    </nav>
    {% block body %}{% endblock %}
    {% block javascripts %}
    {{ encore_entry_script_tags('app') }}
    <script src="{{ asset('js/main.js') }}"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-ENjdO4Dr2bkBIFxQpeoTz1HIcje39Wm4jDKdf19U8gI4ddQ3GYNS7NTKfAdVQSZe"
        crossorigin="anonymous"></script>
    {% endblock %}
</body>

</html>