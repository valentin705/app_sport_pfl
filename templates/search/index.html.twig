{% extends 'base.html.twig' %}

{% block title %}Hello SearchController!{% endblock %}

{% block body %}
<h1>Resultat de la recherche</h1>
{% if app.request.method == 'POST' %}
{% if seances is empty and users is empty %}
<h2>Aucun résultat pour votre recherche</h2>
{% else %}
<div class="container">
    {% if seances is not empty %}
    <h1 class="text-center">Résultats de la recherche pour les séances</h1>
    {% for seance in seances %}
<<<<<<< HEAD
    <div class="mb-3">
        <div class="card">
            <div class="card-body d-flex justify-content-between">
                <div class="">
                    <div class="d-flex align-items-center  pb-3">
                        <div class="d-flex align-items-center">
                            <img class="avatar avatar-2 m-3" src=" {{seance.user.picture}} " alt="">
                            <p>{{ seance.user.username }}</p>
                        </div>
                        <div class="mx-5">
                            <p class="date">{{ seance.createAt | date('d/m/Y') }}</p>
                            <h5>{{ seance.name }}</h5>
                        </div>
                    </div>
                    <div class="d-flex justify-content-between">
                        <img src=" {{seance.picture}} " alt="" class="seance-picture ">
                        <p class="px-3">{{ seance.description }}</p>
                    </div>
                </div>
                <div class="d-flex flex-column justify-content-between">
                    <div class="d-flex justify-content-end">
                        {# <a href="{{ path('update_user_profil', {'id': app.user.id}) }}"><svg
                                xmlns="http://www.w3.org/2000/svg" width="25" height="25" fill="currentColor"
                                class="bi bi-pen" viewBox="0 0 16 16">
                                <path
                                    d="m13.498.795.149-.149a1.207 1.207 0 1 1 1.707 1.708l-.149.148a1.5 1.5 0 0 1-.059 2.059L4.854 14.854a.5.5 0 0 1-.233.131l-4 1a.5.5 0 0 1-.606-.606l1-4a.5.5 0 0 1 .131-.232l9.642-9.642a.5.5 0 0 0-.642.056L6.854 4.854a.5.5 0 1 1-.708-.708L9.44.854A1.5 1.5 0 0 1 11.5.796a1.5 1.5 0 0 1 1.998-.001zm-.644.766a.5.5 0 0 0-.707 0L1.95 11.756l-.764 3.057 3.057-.764L14.44 3.854a.5.5 0 0 0 0-.708l-1.585-1.585z" />
                            </svg></a> #}
                    </div>
                    <div class="d-flex flex-column align-items-end">
                        <p>{{ seance.likes | length}} likes</p>
                        <p>commentaire</p>
                        <a href="{{ path('show_workout', {'id': seance.id}) }}">Voir</a>
                    </div>
                </div>
            </div>
        </div>
=======
    <div>
        <img src="{{ seance.picture }}" alt="">
        <h1>{{ seance.name }}</h1>
        <p>{{ seance.description }} </p>
        par {{ seance.user.username }} <br>
        le {{ seance.createAt | date('d/m/Y') }}
        <a href="{{ path('show_workout', {'id': seance.id}) }}">Voir</a>
>>>>>>> 4501fe71411332186c46fdd6034cb70155ad8043
    </div>
    {% endfor %}
    {% endif %}

    {% if users is not empty %}
    <h1 class="text-center">Résultats de la recherche pour les utilisateurs</h1>
    {% for user in users %}
<<<<<<< HEAD
    <div class="row">
        <div class="card p-4 mb-3">
            <div class="card-body d-flex justify-content-around">
                <div class="d-flex flex-column align-items-center" style="min-width : 150px">
                    <img src="{{ user.picture }}" alt="" class="avatar">
                    <a href="{{ path('user_profil_visited', {'id': user.username })}}">
                        <h4 class="card-title mt-2"> {{ user.username }} </h4>
                    </a>
                </div>
                <div class="pt-4 px-4 mx-4 flex-fill">
                    <p class="font-weight-light">{{ user.description }}</p>
                </div>
                <div style="min-width : 150px">
                    <h5>Sports :</h5>
                    <p>{{ user.sports }}</p>
                </div>
                {# <a href="{{ path('update_user_profil', {'id': user.id}) }}"><svg xmlns="http://www.w3.org/2000/svg"
                        width="25" height="25" fill="currentColor" class="bi bi-pen" viewBox="0 0 16 16">
                        <path
                            d="m13.498.795.149-.149a1.207 1.207 0 1 1 1.707 1.708l-.149.148a1.5 1.5 0 0 1-.059 2.059L4.854 14.854a.5.5 0 0 1-.233.131l-4 1a.5.5 0 0 1-.606-.606l1-4a.5.5 0 0 1 .131-.232l9.642-9.642a.5.5 0 0 0-.642.056L6.854 4.854a.5.5 0 1 1-.708-.708L9.44.854A1.5 1.5 0 0 1 11.5.796a1.5 1.5 0 0 1 1.998-.001zm-.644.766a.5.5 0 0 0-.707 0L1.95 11.756l-.764 3.057 3.057-.764L14.44 3.854a.5.5 0 0 0 0-.708l-1.585-1.585z" />
                    </svg></a> #}
            </div>
        </div>
=======
    <div>
        <img src="{{ user.picture}}" alt="" class="avatar">
        <h1>{{ user.username }}</h1>
        <p>{{ user.description }} </p>
        par {{ user.username }}
>>>>>>> 4501fe71411332186c46fdd6034cb70155ad8043
    </div>
    {% endfor %}
    {% endif %}
</div>
{% endif %}
{% endif %}
{% endblock %}